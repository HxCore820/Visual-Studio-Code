name: 🚀 VS CODE WEB IDX (ZUN ADMIN - RANDOM IP FIX)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h'
        - '5h40m'

jobs:
  VSCode-ZunAdmin:
    runs-on: ubuntu-latest
    steps:
      - name: 🛠️ KHỞI TẠO HỆ THỐNG BIỆT LẬP
        run: |
          sudo hostnamectl set-hostname ZunAdmin
          sudo useradd -m -s /bin/bash Admin
          echo "Admin:Ubuntu@123456" | sudo chpasswd
          sudo usermod -aG sudo Admin

          # Tạo và Mount ổ 60GB
          sudo fallocate -l 60G /mnt/zun_storage.img
          sudo mkfs.ext4 /mnt/zun_storage.img > /dev/null 2>&1
          sudo mount -o loop /mnt/zun_storage.img /home/Admin
          sudo mkdir -p /home/Admin/.config/code-server
          sudo cp /etc/skel/.bashrc /home/Admin/
          sudo chown -R Admin:Admin /home/Admin

          # Cài đặt Code-Server
          curl -fsSL https://code-server.dev/install.sh | sh > /dev/null 2>&1
          sudo -u Admin bash -c "cat <<EOF > /home/Admin/.config/code-server/config.yaml
          bind-addr: 127.0.0.1:8080
          auth: password
          password: Ubuntu@123456
          cert: false
          EOF"

          # Chạy VS Code & Tunnel
          sudo -u Admin nohup code-server > /home/Admin/code_server.log 2>&1 &
          
          wget -q https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-linux-amd64.tar.gz
          tar -xzf kami-tunnel-linux-amd64.tar.gz
          chmod +x kami-tunnel
          
          # Chạy không buffer để ghi file liên tục
          stdbuf -o0 ./kami-tunnel 8080 > kami_tunnel.txt 2>&1 &

      - name: 🎉 THÔNG TIN TRUY CẬP (AUTO DETECT IP)
        run: |
          echo "📡 Đang dò tìm IP random từ hệ thống..."
          sleep 15
          
          # CÔNG THỨC MỚI:
          # 1. Tìm dòng chứa chữ Public
          # 2. Xóa hết các mã màu (sed -r)
          # 3. Lấy dãy số có dạng X.X.X.X:YYYY (grep -EP)
          RAW_IP=$(cat kami_tunnel.txt | sed -r "s/\x1B\[([0-9]{1,3}(;[0-9]{1,2})?)?[mGK]//g" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:[0-9]+' | tail -n 1)
          
          if [ -z "$RAW_IP" ]; then
            echo "❌ Không tìm thấy IP trong log. Nội dung file hiện tại:"
            cat kami_tunnel.txt
            exit 1
          fi

          echo ""
          echo -e "\033[1;32m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"
          echo -e "\033[1;35m  🚀 ZUN ADMIN ISOLATION - KẾT NỐI THÀNH CÔNG!\033[0m"
          echo -e "\033[1;32m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"
          echo -e "\033[1;36m  🔗 LINK TRUY CẬP :\033[0m \033[1;37mhttp://$RAW_IP\033[0m"
          echo -e "\033[1;36m  👤 USERNAME      :\033[0m \033[1;37mAdmin\033[0m"
          echo -e "\033[1;36m  🔐 PASSWORD      :\033[0m \033[1;37mUbuntu@123456\033[0m"
          echo -e "\033[1;32m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"
          echo -e "\033[1;33m  💿 STORAGE       : 60GB (Mounted at /home/Admin)\033[0m"
          echo -e "\033[1;32m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"
          echo ""
          echo "WEB_LINK=http://$RAW_IP" >> $GITHUB_ENV

      - name: ⏳ DUY TRÌ
        run: |
          for i in {1..100}; do
            echo -e "\033[1;34m💎 [$(date +%T)] IP ĐANG LIVE: $WEB_LINK\033[0m"
            sleep 300
          done
